# .env

# MongoDB connection string
MONGO_URI=mongodb+srv://nishank:17dec1998@cluster0.xl90nhp.mongodb.net/?appName=Cluster0

# If you’re using Atlas instead, it would look like:
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/frappe?retryWrites=true&w=majority

# Server port
PORT=5001

# Frontend origin allowed by CORS
CLIENT_ORIGIN=http://localhost:3000

# Admin auth (used by /api/admin/login)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=hellojava

# Optional static admin token (leave empty to auto-derive from username/password)
ADMIN_TOKEN=
